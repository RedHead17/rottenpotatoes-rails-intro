<!--  This file is app/views/movies/index.html.erb -->
<h2>All Movies</h2>

<%#  Part 1: Start here... %>

<table class="table table-striped col-md-12" id="movies">
  <thead>
    <tr>
      <th>
        <%= link_to "Movie Title", movies_path(@sorting_hash["title"]), :id => 'title_header' %>
      </th>
      <th>
        <%= link_to "Rating", movies_path(@sorting_hash["rating"]), :id => 'rating_header' %>
      </th>
      <th>
        <%= link_to "Release Date", movies_path(@sorting_hash["release_date"]), :id => 'release_date_header' %>
      </th>
      <th>
        <%= link_to "More Info", movies_path(@sorting_hash["title"]), :id => 'more_info_header' %>
      </th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td>
          <%= movie.title %>
        </td>
        <td>
          <%= movie.rating %>
        </td>
        <td>
          <%= movie.release_date %>
        </td>
        <td>
          <%= link_to "More about #{movie.title}", movie_path(movie) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary' %>
